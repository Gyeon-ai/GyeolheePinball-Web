# 결희 종겜핀볼 웹

결희 퍼스나콘 공을 물리 엔진으로 굴려 오늘의 종합게임이나 당첨자를 뽑는 방송용 웹 핀볼이다. 원본 [Marble Roulette](https://github.com/lazygyu/roulette)를 기반으로 하며 서버나 데이터베이스 없이 정적 파일만으로 동작한다.

## 주요 기능

- 쉼표 또는 줄바꿈으로 최대 200개 후보 입력
- `이름*3` 형식으로 같은 후보의 공 개수 지정
- `이름/3` 형식으로 공 무게 지정
- 첫 번째, 마지막 또는 지정 순위 당첨자 선택
- 여러 핀볼 맵과 경기 중 `1x`·`2x`·`5x` 배속
- 결희 1티어·2티어 구독 퍼스나콘 20종을 이름별로 고정 배정
- 당첨자 확정 후 직접 시작하는 카운트다운 타이머
- `?names=게임1,게임2` URL 매개변수로 명단 자동 입력
- 브라우저 로컬 저장 및 PWA 오프라인 캐시

## 실행과 빌드

Node.js 20 이상과 pnpm이 필요하다.

```powershell
pnpm install
pnpm dev
pnpm build
```

개발 주소는 `http://localhost:1235`이며, 정적 배포 파일은 `dist` 폴더에 생성된다.

## 수집기 연결

Windows 일반판·자동판 수집기의 `PinballUrl`에 배포 주소를 넣으면 기존 `?names=` 명단 전달 기능을 그대로 사용할 수 있다.

## 이미지와 라이선스

핀볼 엔진 코드는 MIT License를 따른다. 결희 아이콘과 퍼스나콘의 권리는 원저작권자에게 있다.
